<%= form_for @bet do |f| %>
  <%= f.label :Email %>
  <%= f.text_field :receiver_id %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :terms %>
  <%= f.text_area :body %>

  <%= f.label :wager_id %>
  <%= f.collection_select(:wager_id, Wager.all, :id, :name,
    prompt: true) %>

  <%= f.label :Date %>
  <%= f.text_field :date %>


  <%= f.submit 'Submit Bet' %>
<% end %>
