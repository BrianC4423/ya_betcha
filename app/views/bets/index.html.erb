<div class="all bets">
  <h3>Bets, Bets, Bets</h3>
  <div class="row">
    <div class="accepted-bets small-6 columns panel">
      <h4>Active Bets<h4>
        <ol>
          <% @bets.each do |bet| %>
            <% if bet.accepted? %>
              <li><%= link_to bet.title, bet_path(bet) %></li>
            <% end %>
          <% end %>
        </ol>
    </div>
    <div class="pending-bets small-6 columns panel">
      <h4>Pending Bets</h4>
        <ol>
          <% @bets.each do |bet| %>
            <% if !bet.accepted && !bet.declined %>
              <li><%= link_to bet.title, bet_path(bet) %></li>
            <% end %>
          <% end %>
        </ol>
    </div>
  </div>
        <%= paginate @bets %>
</div>
